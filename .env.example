# Database (SQLite by default for local dev). For Postgres, change provider in prisma/schema.prisma and set a PG URL.
DATABASE_URL="file:./dev.db"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-strong-secret"
AUTH_ADMIN_EMAIL="admin@example.com"
AUTH_ADMIN_PASSWORD="change-me-please"

# Stripe (test keys for dev; keep them empty to disable checkout UI)
STRIPE_SECRET_KEY=""
STRIPE_PUBLIC_KEY=""

# OpenAI (для автогенерации постов и Q&A)
OPENAI_API_KEY=""
