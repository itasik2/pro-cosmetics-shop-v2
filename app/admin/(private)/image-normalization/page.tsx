import ImageNormalizationClient from "./ImageNormalizationClient";

export const dynamic = "force-dynamic";

export default function ImageNormalizationPage() {
  return <ImageNormalizationClient />;
}
